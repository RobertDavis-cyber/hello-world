'use strict'

function greetTheCustomer() {
    let name = prompt('What is your name?');

    return document.write(name);
}

function dayOfMonth() {
    let day = prompt(What number day is it? (1-31)');
    let message = '';
    
    if (day <= 1 {
        message = 'Wow first of the month!;
    } else if (day <= 31) {
        message = 'This month is going by fast!';
    } else if (day < 24) {
        message = "Wow the days are going quick!";
    } else {
        message = "That hour does not exist!";
    }

    return message;
}

function rateMyPage(){
    let rating = prompt(On a scale of 1-5, how embarrassed should I be of this page?"
    for(let i =0; i < rating; I++){
        document.write('img src= "dummies.gif" alt='dummies'/>');
    }
    
}